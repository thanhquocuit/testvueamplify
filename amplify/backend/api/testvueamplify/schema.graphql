type UserProfile @model @auth(rules: [{allow: public}]) {
  id: ID!
  first_name: String
  last_name: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  content: String
}
